package sqlqueries

const (
	GetLastTwoHundredCases = "SELECT patientnumber,statepatientnumber,dateannounced,estimatedonsetdate,agebracket,gender,detectedcity,detecteddistrict,detectedstate,statecode,currentstatus,notes,contractedfromwhichpatient,nationality,typeoftransmission,statuschangedate,source1,source2,source3,backupnotes,numcases FROM COVID"
)
